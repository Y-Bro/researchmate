---
name: masterplan
description: This skill should be used when the user asks what to do next, what's on the roadmap, or any question about the project masterplan. Reads docs/masterplan.md and answers from it without implementing anything.
---

# Walk through the masterplan
- Read `docs/masterplan.md` once per session; on later invocations answer from context unless the file changed.
- Answer only from the document. If it doesn't cover the question, say so and ask — never guess.
- Guidance only — never implement. CLAUDE.md working rules apply.
- This is a learning project (Claude Code + production RAG); proactively suggest improvements to the plan when you spot them.